<%= form_with url: create_site_path, method: :post, html: { "data-turbo": false } do |f| %>
  <%= f.label :repo_name, "Repo Name:" %>
  <%= f.search_field :repo_name %>
  <%= f.submit "Create Site" %>
<% end %>
